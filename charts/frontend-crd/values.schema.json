{
  "$schema": "http://json-schema.org/draft-07/schema",
  "title": "frontend-crd values",
  "description": "Dependency-wrapper for the upstream frontend-crd chart (aliased to `crds`). No tunables.",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "global": { "type": "object", "title": "Global values" },
    "crds": { "type": "object", "title": "Upstream frontend-crd passthrough (empty)" }
  }
}
